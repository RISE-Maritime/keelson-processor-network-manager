version: "3.9"

services:

  zenoh-router:
    build: .
    container_name: keelson-processor-whoami
    restart: unless-stopped
    network_mode: "host"
    privileged: true
    command: "--log-level 10 --realm rise --entity-id ted --trigger ping --ping-common-key rise/v0/boatswain"